<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50">
    <div class="container mx-auto py-8 px-4">
        <!-- Header Section -->
        <div class="flex items-center justify-between mb-6">
            <!-- Clickable Header Logo -->
            <a href="/" class="text-2xl font-extrabold text-gray-800 hover:text-blue-500 transition-all duration-200">
                RR Movies
            </a>
        </div>

        <!-- Layout for Large Devices -->
        <div class="lg:flex lg:space-x-8">
            <!-- Video Player Section -->
            <div class="flex-1 bg-white rounded-lg shadow-lg p-6">
                <video controls class="w-full rounded-lg">
                    <source src="<%= movie.filePath %>" type="video/mp4">
                    Your browser does not support the video tag.
                </video>
            </div>

            <!-- Movie Details Section -->
            <div class="lg:w-1/3 mt-8 lg:mt-0">
                <h1 class="text-4xl font-extrabold text-gray-800 mb-4"><%= movie.title %></h1>
                <p class="text-gray-600 text-lg leading-relaxed mb-6"><%= movie.description %></p>
                <a href="/" class="text-lg font-medium text-white bg-blue-500 hover:bg-blue-600 py-2 px-6 rounded-lg shadow inline-block">
                    &larr; Back to Home
                </a>
            </div>
        </div>
    </div>
</body>

</html>
